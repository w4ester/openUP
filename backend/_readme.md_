# Flexie

[![PyPI](https://img.shields.io/pypi/v/wandb-flexie.svg)](https://pypi.org/project/wandb-flexie/)
[![Changelog](https://img.shields.io/github/v/release/wandb/flexie?include_prereleases&label=changelog)](https://github.com/wandb/flexie/releases)
[![License](https://img.shields.io/badge/license-Apache%202.0-blue.svg)](https://github.com/wandb/flexie/blob/main/LICENSE)

A backend service for generating HTML components with AI

## Installation

Clone this repo, then install using `pip`. You'll probably want to create a virtual env.

```bash
git clone https://github.com/wandb/flexie
cd flexie/backend
pip install .

Usage
To start the Flexie backend service, run:
python -m flexie


Docker
You can build and run the Docker file from the /backend directory:

bash
Copy code
docker build . -t wandb/flexie --load
docker run -p 7878:7878 wandb/flexie


Development
First be sure to install the package as editable, then passing --dev as an argument will live reload any local changes.

bash
Copy code
pip install -e .
python -m flexie --dev


Now install the dependencies and test dependencies:

bash
Copy code
pip install -e '.[test]'


To run the tests:

bash
Copy code
pytest

Evaluation
The eval folder contains scripts for evaluating the performance of a model. It automates generating UI, taking screenshots of the UI, then asking gpt-4-vision-preview to rate the elements. More details about the eval pipeline coming soon...

